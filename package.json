{
  "name": "aurelius-stock-predictor",
  "version": "1.0.0",
  "description": "AI-driven stock price prediction — Python backend",
  "private": true,
  "scripts": {
    "build": "echo 'Python project — no npm build required'",
    "start": "echo 'Start via Procfile with gunicorn'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
